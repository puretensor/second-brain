{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd ~/pureMind && git add memory/ knowledge/ projects/ templates/ daily-logs/ CLAUDE.md .gitignore 2>/dev/null; if ! git diff --cached --quiet 2>/dev/null; then git commit -m \"auto: $(date +%Y-%m-%d_%H:%M) vault update\"; fi"
          }
        ]
      }
    ]
  }
}
